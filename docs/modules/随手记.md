# 随手记模块

> 所属项目：Elephant Todo App  
> 最后更新：2026-02-26

---

## 一、功能说明

随手记是轻量级的想法记录，与 Todo 存在**递进关系**：用户随手记下的内容可以独立存在，也可以关联到某个 Todo。多个随手记可以关联到同一个 Todo，帮助用户追溯一个待办事项的思考过程。

| 功能         | 说明                                       |
| ------------ | ------------------------------------------ |
| 记录随手记   | 通过统一输入入口创建，或在随手记列表页直接新建 |
| 关联到 Todo  | 一个随手记可以关联到一个已有的 Todo（多对一） |
| 独立存在     | 随手记可以暂不关联 Todo，后续再关联或转化 |
| 转化为 Todo  | 未关联的随手记可以一键转化为新 Todo     |
| 关联 Todo 显示 | 随手记列表中，已关联的随手记显示关联的 Todo 标题，点击可跳转到 Todo 详情；未关联的显示"未关联"标签 |
| 随手记列表   | 独立的随手记列表页，查看所有随手记，按时间排序 |
| 筛选         | 按关联状态筛选（已关联 Todo / 未关联）；按来源筛选（文字 / 语音） |

---

## 二、API 接口

| 方法   | 路径                         | 说明               | 认证 |
| ------ | ---------------------------- | ------------------ | ---- |
| GET    | /api/ideas                   | 获取 Idea 列表（支持筛选/分页） | 是 |
| POST   | /api/ideas                   | 创建 Idea          | 是   |
| GET    | /api/ideas/:id               | 获取单个 Idea      | 是   |
| PUT    | /api/ideas/:id               | 更新 Idea          | 是   |
| DELETE | /api/ideas/:id               | 删除 Idea          | 是   |
| POST   | /api/ideas/:id/link          | 将 Idea 关联到 Todo（body: {todoId}） | 是  |
| POST   | /api/ideas/:id/unlink        | 取消 Idea 与 Todo 的关联 | 是 |
| POST   | /api/ideas/:id/convert       | 将 Idea 转化为新 Todo | 是 |

---

## 三、数据库表

### 3.1 随手记表（ideas）

| 字段       | 类型         | 约束                    | 说明               |
| ---------- | ------------ | ----------------------- | ------------------ |
| id         | INT          | PRIMARY KEY, AUTO_INCREMENT | 主键，自增     |
| user_id    | INT          | NOT NULL, FOREIGN KEY   | 外键 → users.id    |
| todo_id    | INT          | FOREIGN KEY, NULL       | 外键 → todos.id（可为空，未关联时为 NULL） |
| content    | TEXT         | NOT NULL                | 随手记内容         |
| source     | ENUM('text','voice') | NOT NULL, DEFAULT 'text' | 来源方式（手动输入/语音输入） |
| embedding  | JSON         | NULL                    | 语义向量（预留字段） |
| created_at | DATETIME     | NOT NULL, DEFAULT NOW() | 创建时间           |
| updated_at | DATETIME     | NOT NULL, DEFAULT NOW() ON UPDATE NOW() | 更新时间 |

**索引**：
- `idx_ideas_user_id` — INDEX(user_id)
- `idx_ideas_todo_id` — INDEX(todo_id)
- `idx_ideas_user_created` — INDEX(user_id, created_at)
- `idx_ideas_content` — FULLTEXT(content)

**外键**：
- `fk_ideas_user` — user_id → users(id) ON DELETE CASCADE
- `fk_ideas_todo` — todo_id → todos(id) ON DELETE SET NULL

---

## 四、建表 SQL

```sql
CREATE TABLE ideas (
  id         INT                        AUTO_INCREMENT PRIMARY KEY,
  user_id    INT                        NOT NULL,
  todo_id    INT                        DEFAULT NULL,
  content    TEXT                       NOT NULL,
  source     ENUM('text','voice')       NOT NULL DEFAULT 'text',
  embedding  JSON                       DEFAULT NULL,
  created_at DATETIME                   NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME                   NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_ideas_user_id (user_id),
  KEY idx_ideas_todo_id (todo_id),
  KEY idx_ideas_user_created (user_id, created_at),
  FULLTEXT KEY idx_ideas_content (content),
  CONSTRAINT fk_ideas_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_ideas_todo FOREIGN KEY (todo_id) REFERENCES todos(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 五、相关文件

| 类型 | 路径 |
| ---- | ---- |
| API 路由 | `server/api/ideas/` |
| Model | `server/utils/models/idea.model.ts` |
| Pinia Store | `stores/ideas.ts` |
| 页面 | `pages/ideas/index.vue`（列表）、`pages/ideas/[id].vue`（详情） |
