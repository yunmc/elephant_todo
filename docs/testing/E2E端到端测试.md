# E2E 端到端测试用例

> 所属项目：Elephant Todo App  
> 测试框架：Playwright  
> 浏览器：Chromium（Mobile Chrome + Desktop Chrome）  
> 设备模拟：Pixel 5（移动端）+ Desktop Chrome（桌面端）  
> 最后更新：2026-02-27

---

## 基础设施

### 运行方式

```bash
# 确保开发服务器已启动 (端口 3001)
npm run dev

# 运行 E2E 测试
npm run test:e2e

# 运行指定文件
npx playwright test tests/e2e/auth.spec.ts

# UI 模式调试
npx playwright test --ui
```

### 配置说明

| 项目 | 值 |
|------|-----|
| baseURL | `http://localhost:3001` |
| 测试目录 | `tests/e2e/` |
| 共享 fixtures | `tests/e2e/fixtures/auth.fixture.ts` |
| 测试账号 | 每次运行自动注册临时用户（无需手动配置） |
| 并行策略 | 文件间并行，文件内串行 |
| 超时 | 单测 30s，导航 15s |

### 文件结构

```
tests/e2e/
├── fixtures/
│   └── auth.fixture.ts     # 自动注册+登录 fixture，提供 authedPage
├── auth.spec.ts             # 用户认证（注册/登录/错误/登出/表单验证）
├── todo.spec.ts             # Todo CRUD + 子任务 + 筛选 + 优先级/分类/标签
├── ideas.spec.ts            # 随手记 CRUD + 关联/取消关联/转为待办 + 搜索
├── vault.spec.ts            # 密码本完整流程（解锁/分组/条目CRUD/加密解密）
├── finance.spec.ts          # 记账 CRUD + 分类管理 + 月度统计
├── important-dates.spec.ts  # 重要日期 CRUD + 倒计时 + 每年重复
├── period.spec.ts           # 经期记录 CRUD + 预测
├── navigation.spec.ts       # 底部导航 + 更多页面 + 快速添加
├── settings.spec.ts         # 分类管理 + 标签管理 + 修改密码 + 主题
└── theme.spec.ts            # 主题切换 + 持久化
```

### 选择器策略

项目未使用 `data-testid`，采用以下策略：

| 优先级 | 选择器 | 示例 |
|--------|--------|------|
| 1 | `getByRole('button', { name })` | 按钮操作 |
| 2 | `getByPlaceholder(...)` | 输入框 |
| 3 | `getByText(...)` | 标签/Tab/链接文字 |
| 4 | `locator('.css-class')` | 结构元素（`.todo-item`, `.record-card` 等） |

---

## 一、用户认证流程 — `auth.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-A01 | 未登录重定向 | 直接访问 `/` | 自动重定向到 `/login` |
| E2E-A02 | 登录页要素 | 访问 `/login` | 显示邮箱、密码输入框和登录按钮 |
| E2E-A03 | 错误密码登录 | 输入错误密码 → 点击登录 | 显示错误提示，停留在登录页 |
| E2E-A04 | 注册页要素 | 访问 `/register` | 显示用户名、邮箱、密码输入框 |
| E2E-A05 | 注册 → 自动登录 | 填写注册表单 → 提交 | 成功注册并跳转到首页 |
| E2E-A06 | 忘记密码 | 输入邮箱 → 点击发送 | 显示发送成功提示 |
| E2E-A07 | 登出 | 设置页点击「退出登录」 | 跳转到登录页；再访问 `/` 仍被重定向 |
| E2E-A08 | 登录页导航链接 | 点击「注册」链接 → 再点击「登录」链接 | 正确在登录/注册页间切换 |
| E2E-A09 | 注册密码不匹配 | 输入不同的密码与确认密码 → 提交 | 停留在注册页，不跳转 |

---

## 二、Todo 完整流程 — `todo.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-T01 | 创建 Todo | 快速添加 → 输入标题 → 点击「新建待办」 | Todo 列表出现新记录 |
| E2E-T02 | Todo 详情编辑 | 点击 Todo → 修改标题和描述 → 保存 | 返回列表后显示更新内容 |
| E2E-T03 | 完成/恢复 | 点击勾选圈完成 → 切换到「已完成」Tab 可见 → 再次勾选恢复 | 状态正确切换 |
| E2E-T04 | 删除 Todo | Todo 详情点击「删除这个待办」→ 确认 | Todo 从列表消失 |
| E2E-T05 | 子任务 CRUD | Todo 详情 → 添加子任务 → 勾选完成 → 删除 | 进度条更新，子任务操作正确 |
| E2E-T06 | 设置优先级 | Todo 详情 → 选择优先级「高」→ 保存 → 返回列表 | 列表中显示「高」优先级标签 |
| E2E-T07 | 搜索 | 输入搜索关键词 | 列表只显示匹配的 Todo |
| E2E-T08 | 内联创建分类 | Todo 详情 → 分类下拉 → 输入新分类名 → 添加 → 保存 | 分类创建成功并关联到 Todo |
| E2E-T09 | 优先级筛选 | 列表页打开筛选面板 → 选择「高」优先级 | 列表只显示高优先级的 Todo |

---

## 三、随手记 — `ideas.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-I01 | 创建随手记 | 快速添加 → 输入内容 → 「保存为随手记」 | 随手记列表出现新记录 |
| E2E-I02 | 编辑随手记 | 点击随手记 → 修改内容 → 保存 | 内容更新成功 |
| E2E-I03 | 转为待办 | 随手记详情 → 「转为待办」 | 创建关联 Todo，显示关联标题 |
| E2E-I04 | 取消关联 | 已关联的随手记 → 点击「取消」解除关联 | 关联解除，恢复"关联/转为待办"按钮 |
| E2E-I05 | 删除随手记 | 随手记列表或详情删除 | 随手记消失 |
| E2E-I06 | 搜索随手记 | 输入搜索关键词 → 等待搜索 | 只显示匹配内容的随手记 |

---

## 四、记账 — `finance.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-F01 | 新建分类 | 记账页 → 管理分类 → 新建支出分类 | 分类列表出现新记录 |
| E2E-F02 | 记一笔支出 | 点击「记一笔」→ 填写金额/分类/日期 → 保存 | 记录出现在列表，月度支出金额更新 |
| E2E-F03 | 记一笔收入 | 选择收入类型 → 填写 → 保存 | 列表显示收入记录，月度收入更新 |
| E2E-F04 | 类型筛选 | 切换「支出」/「收入」/「全部」Tab | 列表正确过滤 |
| E2E-F05 | 月份切换 | 点击左/右箭头切换月份 | 统计与列表更新为相应月份 |
| E2E-F06 | 删除记录 | 点击删除 → 确认 | 记录消失，统计金额更新 |

---

## 五、重要日期 — `important-dates.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-D01 | 新建日期 | 点击「添加重要日期」→ 填写标题/日期 → 添加 | 列表显示新日期卡片，倒计时天数正确 |
| E2E-D02 | 编辑日期 | 点击日期卡片 → 修改标题 → 保存 | 标题更新，卡片显示新标题 |
| E2E-D03 | 每年重复 | 创建启用「每年重复」的日期 | 卡片显示「每年」标签 |
| E2E-D04 | 删除日期 | 编辑弹框点击「删除」 | 日期卡片消失 |

---

## 六、经期追踪（多人）— `period.spec.ts`

经期追踪支持为多个人记录经期数据，每人的记录和预测相互独立。

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-P01 | 记录经期（默认人） | 点击「+ 记录经期」→ 选流量 → 记录 | 列表出现新记录卡片，flow badge 正确 |
| E2E-P02 | 编辑经期 | 点击记录卡片 → 修改流量 → 保存 | flow badge 更新 |
| E2E-P03 | 删除经期 | 编辑弹框点击「删除」 | 记录卡片减少 |
| E2E-P04 | 周期预测 | 录入 ≥ 2 条记录（API）→ 查看预测卡片 | 预测卡片或空提示可见 |
| E2E-P05 | 添加新人 | 点击 `+` → 输入名称 → 确认 | 人员切换栏出现新 pill，自动切换 |
| E2E-P06 | 为新人记录经期 | 在新人视图下调 API 创建记录 → 刷新页面 | 新人列表出现记录卡片 |
| E2E-P07 | 切换人员数据隔离 | 切换回默认人「我」 | 看到的是「我」的记录（而非新人的） |

---

## 七、导航与快速添加 — `navigation.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-N01 | 底部导航 Tab | 依次点击「待办」「随手记」「密码本」「更多」 | 正确跳转到对应页面 |
| E2E-N02 | 更多页面跳转 | 点击更多页面各卡片 | 正确跳转到记账/重要日期/经期/设置 |
| E2E-N03 | 快速添加弹框 | 点击底部「＋」按钮 | 弹出快速添加弹框 |

---

## 八、设置 — `settings.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-S01 | 分类管理 | 添加分类 → 删除分类 | 分类列表正确更新 |
| E2E-S02 | 标签管理 | 添加标签 → 删除标签 | 标签列表正确更新 |
| E2E-S03 | 修改密码 | 填写旧密码 + 新密码 → 确认 → 用新密码登录 | 密码修改成功，新密码可登录 |
| E2E-S04 | 主题切换 | 切换暗色/浅色 | `<html>` data-theme 正确变更 |

---

## 九、密码本 — `vault.spec.ts`

密码本使用客户端 AES-256-GCM 加密，主密码永不发送到服务器。每用户独立 salt (PBKDF2)。

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-V01 | 锁定屏幕 | 登录后访问 `/vault` | 显示「密码本已锁定」锁屏和主密码输入框 |
| E2E-V02 | 解锁密码本 | 输入主密码 → 点击「解锁」 | 显示密码本页面标题「密码本」和操作按钮 |
| E2E-V03 | 创建分组 | 点击「+ 分组」→ 输入名称 → 创建 | 分组 chip 出现 |
| E2E-V04 | 创建条目 | 点击「+ 条目」→ 填写名称/用户名/密码 → 创建 | 条目出现在列表中 |
| E2E-V05 | 快速解密 | 点击条目的 👁️ 按钮 | 显示解密后的用户名和密码 |
| E2E-V06 | 搜索条目 | 输入搜索关键词 | 列表只显示匹配的条目 |
| E2E-V07 | 分组筛选 | 点击分组 chip | 列表只显示该分组的条目 |
| E2E-V08 | 条目详情 | 点击条目 → 输入主密码解锁 | 显示解密后的账户信息 |
| E2E-V09 | 编辑条目 | 详情页点击「编辑」→ 修改名称 → 保存 | 条目名称更新 |
| E2E-V10 | 删除条目 | 详情页点击「删除」→ 确认 | 跳转回列表，条目消失 |

---

## 十、主题切换与持久化 — `theme.spec.ts`

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-TH01 | 切换暗色主题 | 设置页点击「深色」 | `<html>` data-theme="dark"，「深色」按钮高亮 |
| E2E-TH02 | 切换浅色主题 | 先切暗色 → 再点击「浅色」 | `<html>` data-theme="light"，「浅色」按钮高亮 |
| E2E-TH03 | 暗色主题跨页持久化 | 切暗色 → 导航到待办 → 返回设置 | 主题保持暗色，按钮仍高亮 |
| E2E-TH04 | 浅色主题跨页持久化 | 切浅色 → 导航到随手记 → 返回设置 | 主题保持浅色 |
| E2E-TH05 | 同 Tab 重复点击持久化 | 切暗色 → 导航到待办 → 再次点击待办 Tab | 主题保持暗色 |

---

## 十一、PWA — `auth.spec.ts`（附加）

| 编号 | 场景 | 步骤 | 预期结果 |
| ---- | ---- | ---- | -------- |
| E2E-W01 | Manifest 可访问 | GET `/manifest.webmanifest` | 返回 200 |
| E2E-W02 | Meta 标签 | 登录页 | `<meta name="theme-color">` 存在 |

---

## 测试数据策略

- **自动注册临时用户**：`auth.fixture.ts` 在首次调用时通过 API 注册临时账号（`e2e_{timestamp}@test.com`），获取 JWT，使用 `page.context().addCookies(...)` 注入认证状态
- **测试间隔离**：每个 spec 文件使用独立用户，不同模块数据互不干扰
- **无需清理**：测试用户使用唯一邮箱，不影响真实数据
- **模块 spec 中正序操作**：创建 → 编辑 → 删除，形成完整生命周期
- **密码本测试**：使用固定主密码（`TestMaster123!`），加密/解密均在浏览器内完成，E2E 测试可自然触发

## 测试统计

| 文件 | 测试数 | 说明 |
|------|--------|------|
| auth.spec.ts | 11 | 认证 A01-A09 + PWA W01-W02 |
| todo.spec.ts | 9 | Todo CRUD T01-T09 |
| ideas.spec.ts | 6 | 随手记 CRUD I01-I06 |
| vault.spec.ts | 10 | 密码本 V01-V10 |
| finance.spec.ts | 6 | 记账 F01-F06 |
| important-dates.spec.ts | 4 | 重要日期 D01-D04 |
| period.spec.ts | 7 | 经期 P01-P07 |
| navigation.spec.ts | 3 | 导航 N01-N03 |
| settings.spec.ts | 4 | 设置 S01-S04 |
| theme.spec.ts | 5 | 主题 TH01-TH05 |
| **合计** | **65** | |
